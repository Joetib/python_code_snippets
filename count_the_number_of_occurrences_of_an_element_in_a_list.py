# To count the number of occurrences of an element in a list,
# we can use the count() method of the list.

my_list = [1, 2, 3, 4, 2, 2, 3, 1, 2]
element = 2

count = my_list.count(element)
print(count)

# Output
# >> 4
# Please Like and Subscribe