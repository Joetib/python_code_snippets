# To calculate the average of all elements in a list, we can use the sum() function to add up all the elements and then divide the sum by the length of the list.

numbers = [1, 2, 3, 4, 5]
average = sum(numbers) / len(numbers)
print(average)

# Output
# >> 3.0
# Please Like and Subscribe