# Let's define two lists
list1 = [1, 2, 3, 4, 5]
list2 = [4, 5, 6, 7, 8]

# Let's find the common elements between the two lists
common_elements = list(set(list1) & set(list2))

# Let's output the common elements
print(common_elements)
# Please Like and Subscribe