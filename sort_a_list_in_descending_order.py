# To sort a list in descending order, you can use the `sorted` function
# with the `reverse` parameter set to True.

my_list = [5, 2, 8, 1, 9]
sorted_list = sorted(my_list, reverse=True)

print(sorted_list)
# Output: [9, 8, 5, 2, 1]
# Please Like and Subscribe