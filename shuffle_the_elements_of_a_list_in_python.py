# To shuffle the elements of a list in Python, you can use the `random` module.
# The `random.shuffle()` function randomly reorders the elements of a list.

import random

my_list = [1, 2, 3, 4, 5]
random.shuffle(my_list)

print(my_list)

# Output:
# [3, 2, 5, 1, 4]
# Please Like and Subscribe