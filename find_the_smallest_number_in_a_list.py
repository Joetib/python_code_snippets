# To find the smallest number in a list, we can use the built-in min() function in Python.
# The min() function returns the smallest element in a given iterable.

numbers = [5, 2, 9, 1, 7]
smallest_number = min(numbers)
print(smallest_number)

# Output
# >> 1
# Please Like and Subscribe