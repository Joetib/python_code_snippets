# To calculate the average of a list, we can use the sum() function
# and divide it by the length of the list.

numbers = [1, 2, 3, 4, 5]
average = sum(numbers) / len(numbers)
print(average)

# Output
# >> 3.0
# Please Like and Subscribe