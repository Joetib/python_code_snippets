# To remove duplicates from a list, we can convert the list to a set and then convert it back to a list.

# Example:
lst = [1, 2, 3, 2, 4, 3, 5]
lst = list(set(lst))
print(lst)

# Output:
# [1, 2, 3, 4, 5]
# Please Like and Subscribe